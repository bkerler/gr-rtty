# auto-generated by grc.converter

id: rtty_decode_ff
label: RTTY Decode
category: '[RTTY]'

parameters:
-   id: samp_rate
    label: Sample rate
    dtype: real
    default: samp_rate
-   id: baud_rate
    label: Baud rate
    dtype: real
    default: '45.45'
-   id: polarity
    label: Polarity
    dtype: bool
    default: 'True'
    options: ['True', 'False']
    option_labels: [Mark high, Mark low]

inputs:
-   domain: stream
    dtype: float

outputs:
-   domain: stream
    dtype: byte

templates:
    imports: import gnuradio.rtty as rtty
    make: |-
        rtty.rtty_decode_ff(
        ${samp_rate},
        ${baud_rate},
        ${polarity},
        )

file_format: 1
